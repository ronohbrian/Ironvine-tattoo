# This file is used automatically by `docker compose up`.
# It's already filled in with working local-dev defaults — the app runs
# immediately. The only thing missing is Stripe, which is optional until
# you actually want to test a real payment (see the root README).

SECRET_KEY=rm74IWpLzpDhnpZxIAlBuhmk9fnws8FCSd8WeL8araGKzh_aC2vnOSpveOO7LIkF
DATABASE_URL=sqlite:////app/data/tattoo.db
CORS_ORIGINS=http://localhost:8080
FRONTEND_URL=http://localhost:8080

# --- Stripe (optional to start) ---
# Leave these blank and everything works except the payment step, which
# will show a friendly "Stripe isn't configured yet" message instead of
# crashing. Fill these in (see root README) when you're ready to test
# real payments with Stripe's test card numbers.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
